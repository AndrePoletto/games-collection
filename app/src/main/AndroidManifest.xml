<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.edu.utfpr.alunos.andrepoletto.gamescollection">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.DayNight">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".AboutActivity"
            android:label="@string/secondActivityTitle" />
        <activity
            android:name=".GameActivity"
            android:label="@string/fourthActivityTitle" />
        <activity
            android:name=".AddProducerActivity"
            android:label="@string/fifthActivityTitle"></activity>
        <activity android:name=".ViewGameActivity"></activity>
    </application>

</manifest>